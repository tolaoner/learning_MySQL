USE store; #choosing the database for the query
SELECT 1, 2  #retrieve all columns with *
#FROM customers #specify the table here
#WHERE customer_id = 1 #get the row which has 1 as the customer id
#ORDER BY first_name #sorting the data by the chosen column
#Order of these keywords are important, it should be in this order like above
#tabs and spaces are ignored so all can be written in one line